import { ThemeOptions } from "@mui/material/styles";

import { typography, shape, components } from "./themeOptions";

const customTheme: ThemeOptions = {
  typography,
  shape,
  components,
};

export default customTheme;
