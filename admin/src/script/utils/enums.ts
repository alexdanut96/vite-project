enum NavigationPaths {
  home = "/",
  analytics = "/analytics",
  sales = "/sales",
  users = "/users",
  products = "/products",
  login = "/login",
  admin = "/admin",
  //   accountInfo = "/account/:dbid/:aid",
}

export { NavigationPaths };
