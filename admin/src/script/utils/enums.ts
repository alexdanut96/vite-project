enum NavigationPaths {
  home = "/",
  analytics = "/analytics",
  sales = "/sales",
  users = "/users",
  products = "/products",
  //   accountInfo = "/account/:dbid/:aid",
}

export { NavigationPaths };
