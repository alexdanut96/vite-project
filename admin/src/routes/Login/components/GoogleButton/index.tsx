const google_client_id = import.meta.env.VITE_GOOGLE_CLIENT_ID;

// Add a type definition for the Google CredentialResponse

const GoogleButton = () => {
  return <div>asa</div>;
};

export { GoogleButton };
